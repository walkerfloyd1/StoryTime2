import React from 'react'
import PropTypes from 'prop-types'

const Dashboard = props => {
    return (
        <div>
            <h1>
                This is where a user's profile will be
            </h1>
        </div>
    )
}

Dashboard.propTypes = {

}

export default Dashboard
