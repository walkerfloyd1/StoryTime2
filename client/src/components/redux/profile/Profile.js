import React from 'react'
import PropTypes from 'prop-types'

const Profile = props => {
    return (
        <div>
            <h1>
                This is where a User's Profile will be shown
            </h1>
        </div>
    )
}

Profile.propTypes = {

}

export default Profile
